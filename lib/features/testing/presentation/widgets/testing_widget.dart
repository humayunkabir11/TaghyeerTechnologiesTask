import 'package:flutter/material.dart';

class TestingWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('testing Widget')));
  }
}